# Ronda 5 — adversarial sobre Fase 3(a) / C-10, implementación (2026-08-20)

**Objeto:** implementación de la decisión 8 🔒 (ADR-008 aceptado):
`procesar_rollout(desde=…)`, `ciclo(t0=…)` con prefiltro mtime,
`ejecutar(backfill=…)`, `watch --backfill`, SPEC-005 enmendada, enmienda
de ADR-005, riesgo timeout en plan, AGENTS/README, 8 tests nuevos.
**Método:** subagente con contexto fresco; diff completo leído, ADR/spec
contrastados cita por cita, repros empíricos en el venv real (Python
3.9.6), suite ejecutada (87/87 OK al momento de la ronda) y re-ejecutada
con `-W error::ResourceWarning`.
**Veredicto:** APROBADO CON CORRECCIONES — 4 hallazgos (1 ALTO,
1 MEDIO, 2 BAJO), todos corregidos antes del commit.

## Hallazgos y destino

| Id | Nivel | Hallazgo (resumen) | Destino |
|---|---|---|---|
| H1 | ALTO | Timestamp tz-naive (parseable, sin offset) → `TypeError` naive-vs-aware en `_cerrado_desde`, **fuera de todo try**: tumba el ciclo y el proceso `watch` entero; contradecía "no parseable → histórico" (0 casos en el corpus medido, pero robustez no contratada). Reproducido | Corregido: `ts.tzinfo is not None` en la guarda — tz-naive es histórico; test nuevo que antes mataba el ciclo |
| H2 | MEDIO | `_AYUDA` raíz sin `[--backfill]` — única pieza de superficie detrás del ADR | Corregido en `__main__.py` |
| H3 | BAJO | Frontera `ts == t0` sin ancla de regresión (verificado inclusivo a mano) | Test nuevo del cierre exacto en t0 |
| H4 | BAJO | Banner de modos sin test; comentario AGENTS.md desalineado | Test nuevo del banner (mockea `ejecutar`, assert del flag en ambos modos); comentario realineado |

## Retirados por el revisor (constancia)

- Contaminación `turn_context`/proyecto pre-t0 hacia turnos post-t0 —
  `extraer_turnos` resetea la acumulación en cada cierre; la herencia de
  `proyecto` es semántica contratada de SPEC-001.
- Rollout con último cierre pre-t0 y texto posterior acumulado — sin
  cierre no hay `Turno`; el texto se materializa en el cierre post-t0
  con el contenido correcto.
- Falta test de archivo creado post-t0 — cubierto por el test de
  ventana (archivo creado después de t0 con turno viejo).
- Backfill podría no restaurar el comportamiento previo — camino de
  código idéntico al pre-ADR-008; suite antigua intacta.
- Manejo de `OSError` en `stat` — documentado y hecho.
- "ADR-008 promete enmendar su contrato pero f1-contratos.md no se
  tocó" — no existe contrato cli-skopos-watch en F1; frase laxa del
  ADR, no deuda.

## Lo que quedó CIERTO

- Corte por turno **antes** de la dedup: histórico no invitado no
  produce `ResultadoTurno` (≠ `omitido`; nunca consulta a Mongo).
- Turno sin timestamp o con basura → histórico; `Z`→`+00:00` funciona
  en el Python 3.9.6 real.
- Prefiltro mtime sólo optimización (testeado con espía); `>=`
  inclusivo y consistente en ambos filtros.
- La política vive en `ejecutar`/`watch_command`; `ciclo` y
  `procesar_rollout` quedan primitivas con default sin corte.
- Banner a stderr distinguiendo modos; SIGTERM/SIGINT sin trabajo nuevo
  entre ciclos; handlers restaurados.
- Tests: los nuevos prueban lo prometido; `max_ciclos` re-hecho sigue
  probando detención (colgaría si no detuviera); fix de ResourceWarning
  correcto.
- Docs: casos SPEC-005 ↔ código 1:1; enmienda de ADR-005 fechada, con
  puntero a la firma, en el changeset de aceptación (exigencia 2 ✓);
  riesgo timeout 120 s registrado en el plan (exigencia 1 ✓) con la
  cita 2/7 verificable en la remedición.

## Cierre de fase

Tras las correcciones: 90/90 en verde (también con
`-W error::ResourceWarning`). Commit de fase cierra el Hito 8; Fase
3(b) queda gated a evidencia, como el ADR-008 y el plan disponen.
