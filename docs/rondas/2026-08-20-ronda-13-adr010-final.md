# Ronda 13 — últimas correcciones de Pinax + adversarial de cierre (2026-08-20)

**Origen:** instrucción del dueño (canal, FIX-AND-RETRY documental
final antes de firma 🔒): tres correcciones obligatorias + ronda
adversarial fresca obligatoria. Autorización limitada a docs
(ADR-010, f1-specs.md, rondas/, evidencia/, plan); `f1-contratos.md`
intacto; sin código/commit/push.

## Parte 1 — Correcciones de Pinax aplicadas

| # | Corrección | Destino |
|---|---|---|
| 1 | **Selección en dos niveles**: identidad de PRODUCTO vs versión de FORMATO (v1/v2 del mismo producto no compiten en identidad); perfil base parser-codex/v1 = identidad + ausencia de firma incompatible (NO inferido de marcas estructurales); claim de ambigüedad calificado (sólo si el otro producto está registrado; imitador no registrado = límite residual heurístico); nunca selección por orden del registro; 5 casos SPEC nuevos | ADR-010 §1 reestructurado (Nivel A/B + perfil base + ok residual); §3 tabla; SPEC-006 Comportamiento/Errores/Casos |
| 2 | **Protocolo de lectura congelado**: una apertura; N=`fstat` del mismo descriptor; leer exactamente N; short read/IO/UTF-8 ⇒ `entrada_corrupta` (`lectura_corta`); lo añadido tras N ⇒ siguiente ciclo; todo opera sobre ese buffer. Límites declarados: no snapshot atómica; no detecta reescritura concurrente del mismo tamaño; garantía = consistencia de lo materializado; prohibido volver a tamaño+mtime | ADR-010 §5 reescrito; SPEC-006 Comportamiento + Errores + caso |
| 3 | **Forma de `detalle` congelada**: `null \| {codigo (enum de 5), candidatos (ids de ficha, lexicográfico, sin rutas ni contenido)}`; ningún texto libre; agregar codigo exige versión nueva; detalle nunca cambia precedencia | ADR-010 §3 ResultadoParseo + párrafo de forma; SPEC-006 Salidas |

## Parte 2 — Ronda adversarial fresca (subagente independiente)

Atacó los 11 puntos exigidos. Veredicto: **FIX-AND-RETRY** — 5
hallazgos, todos corregidos inmediatamente después:

| Id | Nivel | Hallazgo | Destino |
|---|---|---|---|
| F-1 | BLOCKER | Párrafo residual "Detalles de diagnóstico" en §3 ("no exigen v2, gobernados por la ficha") contradecía la forma congelada de `detalle` — misma clase que el H-1 de la ronda 12 | Eliminado con nota de eliminación en §3 |
| F-2 | MEDIO | La evidencia conservaba el claim sin calificar ("coincidencia ajena ⇒ deteccion_ambigua") | Nota de corrección ronda 13 en la evidencia |
| F-3 | MEDIO | "ronda 13" citada diez veces sin acta (trazabilidad rota) | Esta acta la resuelve |
| F-4 | BAJO | "Marcas de estructura" con doble rol (versión en §1 vs extracción en ficha) | Ficha califica: rol extracción/cierre, no reconocen versión |
| F-5 | BAJO | Exclusión mutua v1↔v2 sólo en prosa ("debe") | Precondición de registro explícita en §8 (sin ella el registro se rechaza) |

### Los 11 puntos, veredicto del revisor (tras los fixes)

1. Coexistencia v1/v2 mismo producto ✓ (agrupación por cli_producto;
   caso SPEC; precondición F-5 ahora explícita)
2. Ambigüedad producto vs versión distinguible ✓ (detalle.codigo;
   precedencia única)
3. Sin selección por orden ✓ (negaciones expresas; perfil base =
   predicado de ausencia, no posición)
4. Imitador no registrado ✓ tras F-2 (calificado en ADR, SPEC y
   evidencia)
5. Honestidad del perfil base ✓ (v1 no inferido de marcas; sin
   obligatorias; inalcanzable salvo retiro)
6. Short read implementable ✓ (fstat del mismo descriptor, read(N),
   len<N cubierto; N=0 ⇒ formato_desconocido)
7. Append tras fstat ✓ (punto 5 no contradice punto 6; re-visita por
   mtime≥t0 de ADR-008 — descubrimiento, no comparación de lecturas)
8. Reescritura mismo tamaño como residual ✓ (declarada, observable
   sólo por sello en re-lecturas)
9. Forma cerrada y saneada de detalle ✓ tras F-1 (enum idéntico ADR↔SPEC;
   candidatos saneado; precedencia intacta)
10. Paridad ADR↔SPEC ✓ (8 campos, códigos, protocolo, dos niveles,
    casos)
11. Stop rules y F1 intacto ✓ (diff --name-only sólo .md autorizados;
    f1-contratos.md exit-code 0; ADR propuesto, firma en blanco; sin
    código/commit/push)

### Retirados por el revisor (constancia)

- Acta ronda 11 con el claim viejo — registro histórico, no se
  retro-edita.
- Prefiltro mtime de ADR-008 vs prohibición tamaño+mtime — usos
  distintos (descubrimiento vs comparación de lecturas).
- "Perfil por defecto" vs "no por defecto" — predicado de ausencia;
  el caso SPEC lo cierra.
- IO/UTF-8 sin código de detalle propio — enum cerrado por diseño;
  `lectura_corta` "cuando aplique".

## Verificaciones (post-fixes)

- `git diff --check` limpio (exit 0).
- `git diff --exit-code -- docs/contratos/f1-contratos.md` → exit 0
  (intacto).
- `git diff --name-only` → sólo `.md` autorizados (ADR-010,
  f1-specs.md, plan, evidencia/, rondas/).
- Suite (convención del plan): **102 descubiertos, OK, 0 saltados**
  (Mongo/Ollama arriba; los saltos de este entorno dependen de esos
  servicios — corrida independiente de Pinax del mismo día: 54
  saltados).
- ADR-010 **propuesto**, firma en blanco. Sin código, commit ni push.

## Riesgos residuales (declarados, aceptados hasta v2 del contrato)

1. Imitador no registrado de una identidad — límite de la detección
   heurística; mitigado por frontera cerrada de predicados y por el
   canario de colisión (`cli` distinto con mismo turn_id ⇒ señal).
2. Reescritura concurrente del mismo tamaño — no detectada en el
   momento; observable por sello P4a en re-lecturas.
3. `version_no_soportada` inalcanzable para parser-codex v1 salvo
   retiro (sin predicados positivos declarados) — honesto y declarado;
   se corrige cuando exista una v2 real que declarar.
