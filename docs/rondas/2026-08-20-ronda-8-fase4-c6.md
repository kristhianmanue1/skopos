# Ronda 8 — adversarial sobre Fase 4 / C-6, implementación (2026-08-20)

**Objeto:** implementación de la decisión 9 🔒 (ADR-009 aceptado: P4a
sello fragmento-only + P5 presupuesto con señal de exclusión + P3
declaración dato-nunca-instrucción, manteniendo P1): `captura`
(`fragmento_sha256`, `_sellar_fragmento`), `analisis`/`almacenamiento`
(propagación y persistencia), `cli` (`_servir_fragmento` con cuatro
estados + `sellado`, chequeo de longitud, tope 64 KiB, marcador;
`query --max` + `excluidos.por_limite`; `reanalizar` completo propaga
sello), CONTRATO cli-skopos-query y documento-mongo v2 enmendados,
SPEC-001/004 enmendadas, 13 tests nuevos.
**Método:** subagente con contexto fresco; reprodujo en vivo los ataques
clave (append posterior, pre-edición del origen, `--max` negativo/0,
corte multibyte, `esperado` negativo, overshoot del marcador,
argparse); suite corrida (99 OK al momento de la ronda).
**Veredicto:** APROBADO CON CORRECCIONES — 8 hallazgos (1 ALTO,
2 MEDIO, 5 BAJO), todos incorporados antes del commit.

## Hallazgos y destino

| Id | Nivel | Hallazgo (resumen) | Destino |
|---|---|---|---|
| H1 | ALTO | `--max` negativo: slicing invertido servía n−|max| resultados y fabricaba excluidos imposibles | Corregido: tipo argparse `≥ 0` + clamp defensivo en `query`; test del rechazo del negativo y del `--max 0` |
| H2 | MEDIO | `offset_fin < offset_inicio` (corrupción en Mongo) → `ValueError` del `read` con traceback en vez de estado | Corregido: `esperado <= 0` → `integridad_fallida`; test nuevo |
| H3 | MEDIO | Marcador del código (`\n…[servidos X de Y bytes]`) ≠ formato del contrato (`…[X de Y bytes]`) — el test validaba contra la constante, no contra el contrato | Corregido: contrato y ADR alineados al formato real |
| H4 | BAJO | El texto truncado excede el tope por el largo fijo del marcador (~55 B) — no documentado | Documentado en contrato y ADR |
| H5 | BAJO | Deriva interna del ADR: la sección pre-decisión anunciaba un estado `integridad_no_verificada` que no existe | Corregido: nota de deriva en Consecuencias; manda "Decisiones de implementación" |
| H6 | BAJO | Corte a límite de byte parte caracteres multibyte → U+FFFD | Documentado (degradación aceptable) |
| H7 | BAJO | Tests faltantes: `--max` cableado, sello `None` en captura, sello asentado por supersede | Los tres añadidos |
| H8 | BAJO | Contrato decía "ausente = legado" pero SPEC-001 permite `None` por archivo ilegible al capturar | Corregido: etiqueta precisa ("legado o captura con archivo ilegible") |

## Retirados por el revisor (constancia)

- "Truncar a 20 por defecto es regresión" — es exactamente P5,
  declarada aditiva en el contrato.
- "reanalizar usa turno.fragmento_sha256 en vez de analisis." —
  equivalentes; `analizar_turno` propaga el campo.
- "excluidos podría dejar que superseded consuman cupo" — verificado:
  el filtro de vigencia corre dentro de `buscar_por_tema`, antes del
  corte.
- "Re-lectura por turno en el sello es costo oculto" — declarada en el
  ADR con medición; el análisis domina por órdenes de magnitud.

## Lo que quedó CIERTO (reproducido en vivo por el revisor)

- El sello hashea exactamente `[offset_inicio, offset_fin)` con el
  mismo rango al capturar y al servir; `offset_fin` incluye la línea de
  cierre.
- Append posterior NO rompe sellos previos (`integro`); edición antes
  del rango desplaza offsets → `integridad_fallida` (correcto y
  deseable; la realidad nueva se sirve vía reanalizar).
- Y-5 cerrado: cuatro estados y sólo esos; nunca bytes no verificados;
  chequeo de longitud también para legados; `origen_perdido` para
  OSError.
- P5 completo: `--max` default 20, corte tras el filtro de vigencia,
  `excluidos.por_limite` siempre presente, orden por relevancia.
- P3 en el contrato (declaración en negrita).
- Persistencia extremo a extremo; vigilante/orquestador intactos;
  retro-sello vía supersede posible (`fragmento_sha256` no es clave
  prohibida); `--solo-redaccion` preserva el sello.
- Docs: criterios de reapertura P2/P4b y restricción de orden del
  piloto registrados; snapshots fechados; 0 documentos verificado en
  vivo.

## Cierre de fase

Tras las correcciones: 102/102 en verde; canal real verificado
(`{"resultados": [], "excluidos": {"por_limite": 0}}`, exit 0). La
restricción de orden del dueño/Pinax queda honrada: el sello existe
**antes** de cualquier ingesta de piloto (colección sigue en 0).
