# Ronda 12 — adversarial sobre el paquete ADR-010 + SPEC-006 tras rondas 11/11b/11c (2026-08-20)

**Objeto:** coherencia interna del paquete tras las tres pasadas de
correcciones de Pinax (base `457d827`, todo sin commitear).
**Método:** subagente con contexto fresco; verificó consistencia entre
pasadas, construyó casos de diagnóstico, re-verificó la evidencia con
código (corpus completo: 616 rollouts, originators, frontera del
predicado contra `codexfoo`/`xcodex-tui`, 80/80 `session_meta` en
línea 1, población Claude 0/253 con `session_meta`, claves de payload
— sin campo de versión de formato), corrió la suite y comprobó stop
rules con `git diff --name-only` (f1-contratos.md intacto).
**Veredicto:** APROBADO CON CORRECCIONES — 7 hallazgos (1 BLOCKER,
1 ALTO, 1 MEDIO, 4 BAJO), todos corregidos antes de la firma 🔒.

## Hallazgos y destino

| Id | Nivel | Hallazgo (resumen) | Destino |
|---|---|---|---|
| H-1 | BLOCKER | La sección Errores de SPEC-006 conservaba la regla vieja («identidad presente, estructuras no declaradas ⇒ version_no_soportada») — contradecía la regla 5 del ADR y permitía doble diagnóstico intra-SPEC para el mismo archivo vivo | Corregido: los cuatro bullets reescritos alineados con §1/§3/§5 del ADR |
| H-2 | MEDIO | Tres bullets más desalineados (formato_desconocido «ninguna marca»; entrada_corrupta sin lectura cortada; ambigua «marcas de ≥2 parsers») | Corregidos en la misma reescritura |
| H-3 | ALTO | La regla 4 (i)–(iii) no instanciada en la ficha parser-codex: `session_meta.payload` no declara versión de formato (verificado con código), no hay firmas de otras versiones ni estructuras obligatorias — `version_no_soportada` inalcanzable salvo retiro, y el caso SPEC de estructura obligatoria era vacío para el único adaptador | Corregido: la ficha lo declara explícitamente («ninguno en v1; sólo alcanzable vía retiro»), con el borde cierre-malformado fijado (se ignora, comportamiento SPEC-001) y la promesa de declarar predicados con evidencia cuando existan |
| H-4 | BAJO | Bordes de `eventos_no_reconocidos` sin definir (¿se cuenta en descartes? ¿type ausente?) | Corregido: sólo en archivos parseados; type ausente/no-string se ignora; disjunto de `descartes_linea` por definición |
| H-5 | BAJO | Reproducción 11c con drift intradía (+1/+1 en recount del propio revisor) — números citados como idénticos | Corregido: nota de drift intradía en la evidencia (snapshots de su corrida, no constantes) |
| H-6 | BAJO | «Primer evento queue-operation» describía a la muestra, no a la población Claude | Corregido: el claim operativo pasa a ser el verificado sobre población completa (0/253 con `session_meta`) |
| H-7 | BAJO | Frase de §4 con rama inalcanzable («formato sin parser ⇒ formato_desconocido») | Corregida: identidad casada sin parser activo ⇒ `version_no_soportada`; sin identidad, no hay nada que fallback-ear |

## Retirados por el revisor (constancia)

- «El acta 11b describe la regla ≥N como vigente» — registro histórico
  de la pasada 2, sustituido explícitamente por la pasada 3.
- «payload.session_id podría ser la fuente adoptada» — ficha
  inequívoca; además verificó que el campo existe en 80/80 (disponible
  es cierto).

## Lo que quedó CIERTO (verificado por el revisor)

- Consistencia entre las tres pasadas (salvo H-1/H-2, corregidos aquí):
  sin restos activos del umbral N, tamaño/mtime, «3 formatos»; las
  menciones restantes son notas históricas etiquetadas.
- Caso compuesto derivable sin ambigüedad del ADR: session_meta codex +
  world_state repetido + línea corrupta + cero cierres ⇒ único
  diagnóstico `ok` con `detalle="identidad_reconocida_sin_cierres"`,
  `eventos_no_reconocidos`=N, `descartes_linea`=1, `turnos`=[].
- Evidencia empírica re-verificada con código: 616 rollouts, originators
  51/63/502 (todos casan la frontera; `codexfoo` y `xcodex-tui`
  rechazados), `cli_version` 616/616, `session_meta` en línea 1 en
  80/80 muestreados, población Claude 0/253.
- Paridad SPEC↔ADR letra a letra de `ResultadoParseo` (8 campos,
  precedencia, detalle).
- Stop rules: diff sólo `.md`; `f1-contratos.md` sin cambios; ADR
  propuesto; `git diff --check` limpio; suite **102 descubiertos, OK,
  0 saltados** (convención aplicada); tamaños dentro de límites.

## Estado

Paquete corregido y estable para presentación al dueño. Sin commit ni
push (ambos esperan autorización nueva y separada del dueño).
